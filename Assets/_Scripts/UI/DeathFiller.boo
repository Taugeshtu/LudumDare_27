import UnityEngine

#===========================================================#===========================================================
class DeathFiller( MonoBehaviour ):
	#===========================================================
	def Update():
		guiTexture.color.a = Player.DeathFill *0.5
	#===========================================================
